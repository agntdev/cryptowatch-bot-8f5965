[
  {
    "name": "Configure quiet hours",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Set local time window for suppressing push alerts"
            }
          }
        ],
        "send": {
          "callback": "configure_quiet_hours"
        }
      }
    ]
  }
]
